#pragma once

constexpr int GRID_SIZE = 250;
constexpr float INTERACTION = 1000000.0f;
constexpr float DT = 0.001;

// TODO:
// show/hide windows showing flow, fft
// adjustable boundaries/barriers
// adjustable gravity
// header only library for computation
// reusable window component for rendering selectable attribute
// on screen help
// render text as density for clock etc
